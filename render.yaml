services:
  - type: web
    name: edusearch-api
    runtime: node
    buildCommand: npm install && npx tsc && node dist/db/seed.js
    startCommand: node dist/index.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
